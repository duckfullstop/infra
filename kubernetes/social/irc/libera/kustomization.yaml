apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- certificate.yaml
- deployment.yaml
- service.yaml
- secret.yaml
- ingress.yaml

namespace: social

configMapGenerator:
- name: pounce-libera
  namespace: ducknet
  files:
  - config/pounce.conf